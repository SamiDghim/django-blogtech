Django:

MVT
 Model : data access layer
 View : business logic
 Template: Presentation layer

 python3
 pip3

virtual env:

1 - brew install python@3.9
2 - python3.9 -m pip install --user virtualenv
3 - virtualenv base
4 - source base/bin/activate : this activate virtual env

➜  studybuddy virtualenv base
created virtual environment CPython3.12.3.final.0-64 in 223ms
  creator CPython3Posix(dest=/Users/samydghim/workspace/django/studybuddy/base, clear=False, no_vcs_ignore=False, global=False)
  seeder FromAppData(download=False, pip=bundle, via=copy, app_data_dir=/Users/samydghim/Library/Application Support/virtualenv)
    added seed packages: pip==24.0
  activators BashActivator,CShellActivator,FishActivator,NushellActivator,PowerShellActivator,PythonActivator

➜  studybuddy source base/bin/activate
(base) ➜  studybuddy source base/bin/activate
(base) ➜  studybuddy deactivate
➜  studybuddy 


5 - install djano:  pip3 install django
6 - now -> "django-admin" is available
(env) ➜  studybuddy django-admin       

Type 'django-admin help <subcommand>' for help on a specific subcommand.

Available subcommands:

[django]
    check
    compilemessages
    createcachetable
    dbshell
    diffsettings
    dumpdata
    flush
    inspectdb
    loaddata
    makemessages
    makemigrations
    migrate
    optimizemigration
    runserver
    sendtestemail
    shell
    showmigrations
    sqlflush
    sqlmigrate
    sqlsequencereset
    squashmigrations
    startapp
    startproject
    test
    testserver


7 - start project:  django-admin startproject studybud
8 - cd to project: cd studybud


project structure:

wsgi: web server getway interface: the actual server
ulrs: routing to our project (list of urls)
asgi: asynchronus
settings:  core project configuration

 9 - create app : python3 manage.py startapp base

10 - inside of base folder we see:
 * Views:  similar as controller : call database
 * Model: configure database

11 - views -> extends, block content endblock content

tags:
{% csrf_token %}
{% if ..... %} ......{%endif%} 

{{}} -> variable
{{...}} | -> filter


<a href="room/{{room.id}}"><h5>{{room.id}} -- {{room.name}}</h5></a>
====
<a href="{% url 'room' room.id %}"><h5>{{room.id}} -- {{room.name}}</h5></a>




database:

- python3 manage.py migrate -> execute/apply migrations

Python models:

class Project(models.Model):
  title = models.CharField()
  description = models.TextField()
  id = models.UUIDField()

==> each attribute inside this class represent a column in database, calss name -> table name



* when we add a new model -> generate a migration:
-->  python3 manage.py makemigrations


* execute pending migrations:
-> python3 manage.py migrate


* create superuser:
- python3 manage.py createsuperuser

*access amdin panel:
 localhost:8000/admin -> 


 Model:

 queryset = ModelName.objects.method()
method: all(), filter(attribute=....),exclude(attribute=....), save(), delete()


* class Topic -> models.ForeignKey(Room)
if room defined after Topic , change Room to 'Room' -> sinon problem


* if on_delete=models.SET_NULL --> must add null:True


ModelFom

class RoomForm(ModelForm): -> this is a class RoomForm inherit from ModelForm





docker:

- docker-compose exec web bash
- docker-compose exec web python manage.py migrate

docker-compose exec web python manage.py createsuperuser
